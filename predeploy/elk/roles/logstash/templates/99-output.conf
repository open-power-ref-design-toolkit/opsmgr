#===============================================================================
output {
    elasticsearch {
        hosts => [
{% for host_name in groups['elasticsearch'] %}
'{{ hostvars[host_name]['ansible_ssh_host'] }}:{{ elasticsearch_http_port }}'
{% if not loop.last %}
,
{% endif %}
{% endfor %}
]
    }
}
#===============================================================================
