[opsmgr]
{% for host in groups['shared-infra_hosts'] %}
{{ hostvars['' + host]['physical_host'] }} ansible_ssh_host={{ hostvars['' + host]['ansible_ssh_host'] }}
{% endfor %}

[standalone]
{% for host in groups['shared-infra_hosts'] %}
{{ hostvars['' + host]['physical_host'] }} ansible_ssh_host={{ hostvars['' + host]['ansible_ssh_host'] }}
{% endfor %}

{% for node, hosts in nodes.iteritems() %}
[ubuntu]
{% for host in hosts %}
{{ host.hostname }} ansible_ssh_host={{ host[opsmgr_iface] }}
{% endfor %}
{% endfor %}

{% for node, hosts in nodes.iteritems() %}
[nvidia_gpu]
{% for host in hosts %}
{{ host.hostname }} ansible_ssh_host={{ host[opsmgr_iface] }}
{% endfor %}
{% endfor %}

