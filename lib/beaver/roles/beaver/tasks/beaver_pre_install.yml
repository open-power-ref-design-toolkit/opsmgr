---
# Copyright 2015, Rackspace US, Inc.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

- name: Install beaver prepare packages
  package:
    name: "{{ item }}"
    state: latest
  register: install_pre_packages
  until: install_pre_packages|success
  retries: 5
  delay: 2
  with_items: "{{ beaver_pre_packages }}"
  tags:
    - beaver-pre-packages
    - beaver-pre-install

- name: Upgrades pip if necessary
  pip:
    name: pip
    state: present
    extra_args: "--upgrade"
  register: upgrade_pip
  until: upgrade_pip|success
  retries: 5
  delay: 2
  tags:
    - beaver-pip-upgrade
    - beaver-pre-install

- name: Create the beaver system group
  group:
    name: "{{ beaver_daemon_group }}"
    state: "present"
    system: "yes"
  when: beaver_daemon_group is defined
        and beaver_daemon_group != 'root'
  tags:
    - beaver-pre-install
    - beaver-create-group

- name: Set the beaver groups in RHEL
  set_fact: beaver_daemon_group_extras='root'
  when: ansible_os_family == "RedHat"

- name: Set /var/log files mode
  file: path=/var/log state=directory mode="g+r" recurse=true
  when: ansible_os_family == "RedHat"

- name: Create the beaver system user
  user:
    name: "{{ beaver_daemon_user }}"
    shell: "/bin/false"
    group: "{{ beaver_daemon_group }}"
    groups: "{{ beaver_daemon_group_extras|default() }}"
    home: "/var/lib/{{ beaver_daemon_user }}"
    system: "yes"
    createhome: "yes"
    uid: "2468"
  when: beaver_daemon_user is defined
        and beaver_daemon_user != 'root'
        and beaver_daemon_group is defined
  tags:
    - beaver-pre-install
    - beaver-create-user

- name: Create beaver directories
  file:
    state: directory
    path: "{{ item }}"
    owner: "{{ beaver_daemon_user }}"
    group: "{{ beaver_daemon_group }}"
  with_items:
    - "/etc/beaver"
    - "/etc/beaver/conf.d"
    - "/var/log/beaver"
  tags:
    - beaver-pre-install
    - beaver-create-directories

- name: Find the existing groups on the system
  shell: 'getent group | cut -d: -f1'
  register: etc_groups

- name: Add the beaver userid to any groups required for monitoring
  user:
    name: "{{ beaver_daemon_user }}"
    groups: "{{ item }}"
    append: yes
  when: '"{{ item }}" in etc_groups.stdout_lines'
  with_items:
      - ceph

