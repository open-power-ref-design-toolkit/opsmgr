---
- hosts: all
  become: true
  roles:
    - provisioned

- hosts: localhost
  become: false
  connection: local
  vars_files:
    - [ "etc/vars.yml", "vars/containers.yml" ]
  roles:
    - provisioner

