---
- hosts: all
  become: true

  pre_tasks:

    - debug: var=ansible_hostname
    - debug: var=ansible_default_ipv4.address

    - name: test provisiong task
      shell: ls
      register: out

    - name: print test result
      debug: var=out.stdout_lines

